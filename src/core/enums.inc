#if defined _AMONGMP_ENUMS_INCLUDED
    #endinput
#endif
#define _AMONGMP_ENUMS_INCLUDED

enum E_GAME_STATE
{
    GAME_STATE_BOOTING,
    GAME_STATE_WAITING,
    GAME_STATE_STARTING,
    GAME_STATE_PLAYING,
    GAME_STATE_MEETING,
    GAME_STATE_ENDING
};

enum E_PLAYER_ROLE
{
    ROLE_NONE,
    ROLE_CREWMATE,
    ROLE_IMPOSTOR
};
